<main class="empty">
  <h1>BranchKit</h1>
  <p>No repo open yet.</p>
</main>

<style>
  .empty {
    height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    background: var(--bg);
    color: var(--text);
  }

  h1 {
    color: var(--accent);
    font-weight: 600;
  }

  p {
    color: var(--text-muted);
  }
</style>
